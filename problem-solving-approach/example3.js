function charCount(str) {
  // return an object with keys in lowercase alphanumeric characters
}

function charCount(str) {
  // make variable to return an object at the end
  // loop over string foreach character
  // if char is a key in object, plus 1 to count
  // else if not in object, create key with value 1
  // return object at end
}
